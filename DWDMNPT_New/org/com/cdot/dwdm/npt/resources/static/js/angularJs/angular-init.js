var app = angular.module("npt-app", []);
 /// ['ngRoute']); in case of multiple controller should go for the ngRoute after defining the route entries 
 

// gScope=function(){
//     function getScope(ctrlName) {
//         var sel = 'div[ng-controller="' + ctrlName + '"]';
//         console.log(angular.element(sel).scope());
//         return angular.element(sel).scope();
//     }
// } 

// var globalScope=gScope();

